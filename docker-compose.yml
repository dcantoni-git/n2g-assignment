version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py wait_for_db &&
            python manage.py migrate &&
            python manage.py runserver 0.0.0.0:8000"
    environment:
      # - DB_HOST=candidaterds.n2g-dev.net
      - DB_HOST=test_db
      - DB_NAME=test_cand_upk1
      - DB_USER=cand_upk1
      - DB_PASS=E66YEUd7b0lozdzd
    depends_on:
      - test_db
      # - db

  # db:
  #   image: mysql:latest
  #   volumes:
  #     - dev-db-data:/var/lib/mysql
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=E66YEUd7b0lozdzd
  #     - MYSQL_DB_HOST=candidaterds.n2g-dev.net
  #     - MYSQL_DB_NAME=cand_upk1
  #     - MYSQL_DB_USERNAME=cand_upk1
  #     - MYSQL_DB_PASSWORD=E66YEUd7b0lozdzd
  #   ports:
  #     - "3306:3306"
  #   # hostname: candidaterds.n2g-dev.net

  test_db:
    image: mysql:latest
    # command: --default-authentication-plugin=mysql_native_password
    environment:
      - MYSQL_ROOT_PASSWORD=E66YEUd7b0lozdzd
      - MYSQL_DATABASE=test_cand_upk1
      - MYSQL_USER=cand_upk1
      - MYSQL_PASSWORD=E66YEUd7b0lozdzd
    volumes:
      - test-db-data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  # dev-db-data:
  test-db-data:


#     environment:
#       - MYSQL_ROOT_PASSWORD=E66
#       - DB_HOST=db
#       - DB_NAME=devdb
#       - DB_USER=devuser
#       - DB_PASS=changeme
#     depends_on:
#       - db

#   db:
#     # image: postgres:13-alpine
#     # volumes:
#     #   - dev-db-data:/var/lib/postgresql/data
#     image: mysql:5.7.38
#     volumes:
#       - dev-db-data:/var/lib/mysql

#     environment:
#       - MYSQL_ROOT_PASSWORD=E66
#       - MYSQL_DB=devdb
#       - MYSQL_USER=devuser
#       - MYSQL_PASSWORD=changeme

# volumes:
#   dev-db-data: